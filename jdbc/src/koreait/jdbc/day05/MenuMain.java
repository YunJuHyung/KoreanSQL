package koreait.jdbc.day05;

public class MenuMain {
	//main 메소드는 `응용프로그램`을 만들기 위해서 작성하는 것으로 테스트용은 아닙니다.
	
	//특정 메소드가 잘 실행되는지 검증(테스트)하는 것을 단위테스트(unit test)라고합니다.
	//자바에서 기본적으로 사용하는 단위테스트 라이브러리로 JUnit 이 있습니다.
	//테스트 케이스는 빌드에 포함시키지 않습니다. src폴더 : 프로덕션 코드.(빌드에 포함되는 코드)
	//우리는 하나의 dao를 완성하기 위해 main까지 만들지 않고도 검증하는 목적으로 사용할 수 있습니다.
	



}
